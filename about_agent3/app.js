let clients = [];
const csv = text => { const rows=[]; let row=[], cell='', quoted=false; for(let i=0;i<text.length;i++){const ch=text[i], next=text[i+1]; if(ch==='"'&&quoted&&next==='"'){cell+='"';i++;} else if(ch==='"'){quoted=!quoted;} else if(ch===','&&!quoted){row.push(cell);cell='';} else if((ch==='\n'||ch==='\r')&&!quoted){if(ch==='\r'&&next==='\n')i++;row.push(cell);if(row.some(v=>v.trim()))rows.push(row);row=[];cell='';} else cell+=ch;} if(cell||row.length){row.push(cell);rows.push(row);} const headers=rows.shift(); return rows.map(r=>Object.fromEntries(headers.map((h,i)=>[h.trim(),(r[i]??'').trim()]))); };
const parseMoney = value => Number(String(value).replace(/[^0-9.-]/g,'')) || 0;
const money = value => '$' + (value/1000000).toFixed(1) + 'M';
const loadData = async () => {
  /* CLOUDFLARE VERSION: uncomment this block when deployed with data_csv available at /data_csv.
  const [clientResponse, portfolioResponse] = await Promise.all([fetch('../data_csv/clients.csv'), fetch('../data_csv/portfolios.csv')]);
  if(!clientResponse.ok || !portfolioResponse.ok) throw new Error('Could not load data_csv files');
  const rawClients=csv(await clientResponse.text()); const rawPortfolios=csv(await portfolioResponse.text());
  */
  // LOCAL VERSION: works when frontend/index.html is opened directly from the filesystem.
  const rawClients=window.LOCAL_DATA.clients; const rawPortfolios=window.LOCAL_DATA.portfolios;
  clients=rawClients.map(c=>({...c,id:c.client_id,name:c.client_name,country:c.country_of_residence,currency:c.base_currency,age:Number(c.age),aum:money(parseMoney(c.total_aum_usd)),risk:c.risk_profile,portfolios:rawPortfolios.filter(p=>p.client_id===c.client_id).map(p=>[p.portfolio_name,p.service_model,money(parseMoney(p.aum_usd_current)),p.portfolio_id])}));
  const total=rawClients.reduce((sum,c)=>sum+parseMoney(c.total_aum_usd),0); document.querySelector('#clientCount').textContent=String(rawClients.length).padStart(2,'0'); document.querySelector('#totalAum').textContent=money(total); document.querySelector('#portfolioCount').textContent=String(rawPortfolios.length).padStart(2,'0');
  const now=new Date(); const reviews=rawClients.filter(c=>{const d=new Date(c.kyc_review_due+'T00:00:00'); return d.getFullYear()===now.getFullYear()&&d.getMonth()===now.getMonth()&&d>=new Date(now.toDateString());}).length; document.querySelector('#reviewCount').textContent=String(reviews).padStart(2,'0');
  render();
};
const fallbackClients = [
  {id:'CL-0001',name:'Hartono Wijaya Kusuma',country:'Singapore',currency:'SGD',aum:'$46.6M',age:34,risk:'Balanced Growth',portfolios:[['Core Discretionary Mandate','Discretionary','$26.9M'],['Legacy Holdings Custody Account','Custody','$19.7M']]},
  {id:'CL-0002',name:'Ravi Chandrasekaran',country:'Singapore',currency:'USD',aum:'$46.7M',age:41,risk:'Growth',portfolios:[['Advisory Growth Portfolio','Advisory','$14.8M'],['Founder Shareholding Custody','Custody','$31.9M']]},
  {id:'CL-0003',name:'Margarethe Voss-Brenner',country:'Singapore',currency:'EUR',aum:'$22.2M',age:58,risk:'Conservative',portfolios:[['Inherited Portfolio — Under Review','Advisory','$22.2M']]},
  {id:'CL-0004',name:'Chalermchai Suphanburi',country:'Thailand',currency:'USD',aum:'$33.5M',age:66,risk:'Income',portfolios:[['Retirement Income Mandate','Discretionary','$33.5M']]},
  {id:'CL-0005',name:'Aishah binti Rahman',country:'Malaysia',currency:'SGD',aum:'$19.4M',age:47,risk:'Sustainable Balanced',portfolios:[['Sustainable Balanced Mandate','Discretionary','$19.4M']]},
  {id:'CL-0006',name:'Nguyen Thi Bao Tran',country:'Singapore',currency:'SGD',aum:'$18.1M',age:39,risk:'Balanced',portfolios:[['Advisory Balanced Portfolio','Advisory','$18.1M']]},
  {id:'CL-0007',name:'Alistair Pemberton-Hale',country:'Singapore',currency:'USD',aum:'$40.0M',age:72,risk:'Balanced',portfolios:[['Core Balanced Mandate','Discretionary','$40.0M']]}
];
const list = document.querySelector('#clientList'); const kycColumn = document.querySelector('#kycColumn'); const meetingColumn = document.querySelector('#meetingColumn'); const empty = document.querySelector('#emptyState'); const search = document.querySelector('#search');
function icon(service){ return service==='Custody'?'▣':service==='Advisory'?'◌':'✦'; }
function render(query=''){ const q=query.toLowerCase().trim(); const today=new Date(); today.setHours(0,0,0,0); const sorted=[...clients].sort((a,b)=>{const ad=new Date(a.kyc_review_due+'T00:00:00'),bd=new Date(b.kyc_review_due+'T00:00:00'); const daysA=Math.ceil((ad-today)/86400000),daysB=Math.ceil((bd-today)/86400000); const af=!Number.isNaN(ad.getTime())&&daysA>=0&&daysA<=62, bf=!Number.isNaN(bd.getTime())&&daysB>=0&&daysB<=62; if(af!==bf)return af?-1:1; if(af&&ad-bd)return ad-bd; return String(a.id).localeCompare(String(b.id));}); const filtered=sorted.map(c=>({...c,portfolios:c.portfolios.filter(p=>!q||c.name.toLowerCase().includes(q)||p[0].toLowerCase().includes(q)||p[1].toLowerCase().includes(q))})).filter(c=>c.portfolios.length||c.name.toLowerCase().includes(q));
 list.innerHTML=filtered.map(c=>`<article class="client-card"><div class="client-head"><div class="client-title"><div><h2 class="client-name">${c.name}</h2><p class="client-meta">${c.id} · ${c.country} · Base currency ${c.currency}</p></div></div><div class="client-actions"><span class="client-aum"><small>Total AUM</small><b>${c.aum}</b><button class="small-talk">Small talk</button></span><button class="collapse" aria-label="Show client details and portfolios">⌄</button></div></div><div class="client-details"><div><span>CLIENT NET WORTH</span><b>${c.aum}</b></div><div><span>AGE</span><b>${c.age}</b></div><div><span>RISK PROFILE</span><b>${c.risk}</b></div></div><div class="portfolio-grid" style="display:none">${c.portfolios.map(p=>`<div class="portfolio"><div class="portfolio-top"><span class="portfolio-icon">${icon(p[1])}</span><span class="portfolio-service">${p[1].toUpperCase()}</span></div><h3>${p[0]}</h3><p>${p[3]}</p><span class="portfolio-aum">${p[2]}</span></div>`).join('')}<button class="portfolio add-portfolio"><span>＋</span><small>Add portfolio</small></button></div></article>`).join(''); empty.hidden=filtered.length>0;
 document.querySelectorAll('.collapse').forEach(btn=>btn.addEventListener('click',()=>{const card=btn.closest('.client-card'); const details=card.querySelector('.client-details'); const portfolios=card.querySelector('.portfolio-grid'); const open=!details.classList.contains('open'); details.classList.toggle('open',open); portfolios.style.display=open?'grid':'none'; btn.textContent=open?'⌃':'⌄';})); }
function addKycAlerts(){const today=new Date();today.setHours(0,0,0,0);document.querySelectorAll('.client-card').forEach(card=>{const name=card.querySelector('.client-name')?.textContent;const c=clients.find(item=>item.name===name);if(!c)return;const d=new Date(c.kyc_review_due+'T00:00:00');const days=Math.ceil((d-today)/86400000);if(!Number.isNaN(d.getTime())&&days>=0&&days<=62){const target=card.querySelector('.client-meta');const tone=days<=31?'urgent':'';target.insertAdjacentHTML('afterend',`<span class="kyc-alert ${tone}">! KYC review due ${d.toLocaleDateString('en-SG',{day:'numeric',month:'short',year:'numeric'})} · ${days===0?'today':`in ${days} days`}</span>`);}})}
function bindClientHeaders(){document.querySelectorAll('.client-head').forEach(header=>header.addEventListener('click',()=>{const card=header.closest('.client-card');const details=card.querySelector('.client-details');const portfolios=card.querySelector('.portfolio-grid');const open=!details.classList.contains('open');document.querySelectorAll('.small-talk-panel.open').forEach(item=>item.classList.remove('open'));document.querySelectorAll('.client-details.open').forEach(item=>item.classList.remove('open'));document.querySelectorAll('.portfolio-grid').forEach(item=>item.style.display='none');if(open)setTimeout(()=>{details.classList.add('open');portfolios.style.display='grid';},300);}));}
function smallTalkCopy(likes){const text=likes?.toLowerCase()||'';if(text.includes('golf'))return 'Scottie Scheffler won the Tour Championship and FedEx Cup on 30 Aug 2026, a major talking point in this weeks golf coverage.';if(text.includes('formula 1'))return 'The Italian Grand Prix is in focus this week, with Ferrari unveiling a Schumacher-inspired Monza livery and teams bringing technical upgrades.';if(text.includes('cycling'))return 'Bastien Tronchon stunned the field to win Vuelta a Espana stage 10 on 1 Sep 2026, while the transfer window continues to reshape the peloton.';if(text.includes('whisky'))return 'September whisky coverage includes a new Loch Lomond peated release, a Dalmore Marsala-finished single malt, and further Macallan Harmony releases.';if(text.includes('sailing'))return 'Recent offshore racing has put weather strategy and crew decisions in the spotlight—ask whether the client has been following the latest results.';if(text.includes('classical')||text.includes('opera'))return 'The latest concert and opera season has brought renewed attention to standout young performers—ask whether the client has seen anything memorable recently.';if(text.includes('fashion'))return 'Sustainable materials and circular design remain a major topic in fashion—ask which brands the client thinks are genuinely making progress.';if(text.includes('cooking')||text.includes('food'))return 'Food tourism is seeing a renewed interest in regional and heritage cooking—ask which destination or dish the client would recommend next.';if(text.includes('skiing'))return 'The latest season has renewed interest in snow conditions and resort sustainability—ask which destination is currently on the client’s list.';if(text.includes('art'))return 'September New York exhibitions include photography by Jeannette Montgomery Barron and a major Iris van Herpen design exhibition.';if(text.includes('tennis'))return 'The latest tournament results have produced some unexpected momentum shifts—ask which player the client thinks is currently in the best form.';if(text.includes('chess'))return 'Recent elite chess coverage has focused on creative opening preparation and increasingly aggressive early-game play.';if(text.includes('cricket'))return 'The 2026 international cricket season continues through September, with the Caribbean Premier League reaching its decisive stages.';if(text.includes('cars'))return 'The luxury-car market continues to focus on limited production and collector provenance—ask which model the client considers most desirable now.';if(text.includes('marathon'))return 'Race calendars are bringing renewed attention to training, pacing, and endurance—ask whether the client has a race or personal target coming up.';if(text.includes('tea')||text.includes('gardening')||text.includes('calligraphy'))return 'Seasonal craft and design events have brought renewed interest to traditional practices—ask whether the client has visited or discovered anything inspiring recently.';return 'Small talk prompts and recent client context will appear here.';}
function interestPrompt(interest){const text=interest.toLowerCase();if(text.includes('horse racing'))return 'Ask whether the client has been following the latest major race results and which horse is showing the strongest form.';if(text.includes('property development'))return 'Ask how the client views current property demand, construction costs, and opportunities in the markets they follow.';if(text.includes('whisky'))return 'Ask whether the client has discovered any interesting bottles or limited releases recently.';return smallTalkCopy(interest);}
function refreshSmallTalkPanels(){document.querySelectorAll('.small-talk-panel').forEach(panel=>{const card=panel.closest('.client-card');const client=clients.find(c=>c.name===card.querySelector('.client-name')?.textContent);const row=window.LOCAL_DATA.likes?.find(l=>l.client_id===client?.id);if(row?.likes){panel.innerHTML='<span>SMALL TALK</span>'+row.likes.split(',').map(interest=>`<div class="talk-topic"><h3>${interest.trim()}</h3><p>${interestPrompt(interest.trim())}</p></div>`).join('');}})}
function bindSmallTalk(){document.querySelectorAll('.small-talk').forEach(button=>{const card=button.closest('.client-card');const client=clients.find(c=>c.name===card.querySelector('.client-name')?.textContent);const likeRow=window.LOCAL_DATA.likes?.find(l=>l.client_id===client?.id);if(!card.querySelector('.small-talk-panel'))card.querySelector('.client-details').insertAdjacentHTML('beforebegin',`<div class="small-talk-panel"><span>SMALL TALK</span><h3>${likeRow?.likes||'Conversation notes'}</h3><p>${likeRow?.likes?smallTalkCopy(likeRow.likes):'Small talk prompts and recent client context will appear here.'}</p></div>`);button.addEventListener('click',event=>{event.stopPropagation();const panel=card.querySelector('.small-talk-panel');const open=!panel.classList.contains('open');document.querySelectorAll('.small-talk-panel.open').forEach(item=>item.classList.remove('open'));document.querySelectorAll('.client-details.open').forEach(item=>item.classList.remove('open'));document.querySelectorAll('.portfolio-grid').forEach(item=>item.style.display='none');if(open)setTimeout(()=>panel.classList.add('open'),300);});});}
function bindPortfolioLinks(){document.querySelectorAll('.portfolio:not(.add-portfolio)').forEach(card=>card.addEventListener('click',()=>{const name=card.querySelector('h3')?.textContent;const clientName=card.closest('.client-card')?.querySelector('.client-name')?.textContent;const client=clients.find(c=>c.name===clientName);const raw=client&&window.LOCAL_DATA.portfolios.find(x=>x.portfolio_name===name&&x.client_id===client.id);if(raw)location.href=`portfolio.html?id=${encodeURIComponent(raw.portfolio_id)}`;}));}
function enableDrag(card){card.draggable=true;card.addEventListener('dragstart',()=>card.classList.add('dragging'));card.addEventListener('dragend',()=>card.classList.remove('dragging'));}
function sortRightColumn(){[...list.children].sort((a,b)=>{const ac=clients.find(c=>c.name===a.querySelector('.client-name')?.textContent),bc=clients.find(c=>c.name===b.querySelector('.client-name')?.textContent);return String(ac?.id).localeCompare(String(bc?.id));}).forEach(card=>list.appendChild(card));}
function organizeBoard(){const today=new Date();today.setHours(0,0,0,0);document.querySelectorAll('#clientList .client-card').forEach(card=>{const c=clients.find(x=>x.name===card.querySelector('.client-name')?.textContent);const d=new Date(c?.kyc_review_due+'T00:00:00');const days=Math.ceil((d-today)/86400000);if(c&&!Number.isNaN(d.getTime())&&days>=0&&days<=62){kycColumn.appendChild(card);}else{enableDrag(card);}});sortRightColumn();}
meetingColumn.addEventListener('dragover',e=>e.preventDefault());meetingColumn.addEventListener('drop',e=>{e.preventDefault();const card=document.querySelector('.dragging');if(card){meetingColumn.appendChild(card);enableDrag(card);}});list.addEventListener('dragover',e=>e.preventDefault());list.addEventListener('drop',e=>{e.preventDefault();const card=document.querySelector('.dragging');if(card){list.appendChild(card);sortRightColumn();}});
search.addEventListener('input',e=>{render(e.target.value);addKycAlerts();organizeBoard();bindClientHeaders();bindSmallTalk();refreshSmallTalkPanels();bindPortfolioLinks()}); loadData().then(()=>{addKycAlerts();organizeBoard();bindClientHeaders();bindSmallTalk();refreshSmallTalkPanels();bindPortfolioLinks()}).catch(error=>{console.error(error); clients=fallbackClients; render();addKycAlerts();organizeBoard();bindClientHeaders();bindSmallTalk();refreshSmallTalkPanels();bindPortfolioLinks()});








